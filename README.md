# SmartSignalAI FYP

Predict network RSRP using ML models (Linear Regression & Random Forest).

## Folder Structure

- `notebooks/` → Jupyter notebooks
- `data/` → raw and processed datasets
- `scripts/` → reusable Python scripts
- `.gitignore` → ignored files